{
  "name": "nuovalingua-functions",
  "version": "1.0.0",
  "description": "Firebase Functions pour NuovaLingua - Extraction de contenu web",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "firebase deploy --only functions",
    "serve": "firebase emulators:start --only functions",
    "lint": "echo 'No linting configured'",
    "build": "echo 'No build step required'"
  },
  "keywords": ["firebase", "functions", "content-extraction", "puppeteer"],
  "author": "NuovaLingua",
  "license": "MIT",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.8.0"
  },
  "engines": {
    "node": "20"
  }
}